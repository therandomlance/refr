# dependencies
node_modules

# next.js
.next
out

# build
dist
redeploy.sh

# prisma generated client
generated

# typescript build info
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# runtime data directory (DB, thumbnails, config, sessions, etc.)
data

# python venv for the CLIP sidecar (auto-created on first enable)
ml/.venv
